# 无限画布一键部署脚本 · 使用说明

项目已拉取到本目录（`infinite-canvas` v0.19.0），并新增 `deploy.sh`
（风格与软路由项目 `auth/license-server/deploy.sh` 对齐）。

## 怎么用

把 `deploy.sh` 传到你的服务器（或直接在服务器上 clone 本仓库），然后：

```bash
bash deploy.sh                    # 交互式菜单
bash deploy.sh --install -y --port 3000     # 一条命令装完
bash deploy.sh --help             # 全部参数
```

## 菜单功能对照（和软路由授权服务器脚本一个套路）

| 授权服务器菜单 | 本脚本对应项 | 说明 |
|---|---|---|
| 2) 更新代码（保留数据） | 2) 更新到最新版 | 重新拉镜像/重建，浏览器用户数据不受影响 |
| 3) 全新安装/再装一套 | 1) 全新安装/再装一套/重新部署 | 换端口换目录即可再装一套 |
| 4) 查看管理员令牌 | 3) 体检 + 看访问地址 | 本项目没有令牌，体检看容器/端口/页面 |
| 8) 体检 + 看管理页地址 | 3) 体检 + 看访问地址 | 容器状态 + HTTP 200 检查 |
| 9) 卸载 | 9) 卸载 | a=保留目录 / b=连目录删除 |
| h) 配 HTTPS | 5) 配 HTTPS | Caddy 反代 + 自动证书（支持直连/中转） |
| w) 改离线窗口 | 4) 改监听端口 | 端口随时换 |
| t) 换 Cloudflare token | 5 配 HTTPS（中转模式会问 CF token） | 走中转/大陆前置时用 DNS-01 |
| 1) 进不去管理页（救急） | 8) 数据说明 | 本项目无管理页、无服务端数据，改为数据说明 |
| — | 6) 开/关统计 | GA4 / 百度统计 |
| — | 7) 换镜像来源 | ghcr.io 拉不动时：本地构建 / 加速镜像 |
| — | l) 看日志 / r) 重启容器 | 运维快捷项 |

## 核心设计点（想清楚了的）

1. **数据全在浏览器，服务器零状态。** 画布、素材、生成记录、API Key 都存在
   localStorage/IndexedDB。所以更新、重装、卸载**永远不会丢用户数据**，
   服务器不需要任何数据卷、不需要备份、不需要迁移。
   —— 这也是菜单比授权服务器简单很多的根本原因。

2. **镜像来源三选一**（菜单 7 / 安装时选）：
   - 官方镜像 `ghcr.io/basketikun/infinite-canvas:latest`（最快，但国内常拉不动）
   - 加速前缀镜像（如 `ghcr.nju.edu.cn` / `ghcr.m.daocloud.io`）
   - 本地源码构建（不依赖 ghcr.io，但要先 clone 源码，装的时候会问）

3. **更新 = 重新拉镜像/重建 + 重启容器**，一条命令 `--update`。
   因为没有服务端数据，「更新」不需要任何数据迁移动作。

4. **先备好镜像再删旧容器**：拉取/构建失败时旧服务不停，避免把自己锁在外面。

5. **非交互可用**：`-y` + `--json` 支持 ansible/CI 调用，无终端不挂死。

## 提醒

- 首次打开页面后，用户需要在右上角配置里填自己的 OpenAI 兼容
  Base URL 和 API Key（存浏览器本地）。
- 一台服务器想给多个用户用：菜单选 1 换个端口再装一套即可。
- 用户换电脑 = 换了个空画布，需要在旧浏览器里「导出」画布文件再导入。